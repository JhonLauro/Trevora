# Trevora Frontend

Create React App frontend matching the Call-A-Mechanic setup.

## Scripts

```bash
npm install
npm start
npm run build
npm test -- --watchAll=false
```

The app runs on `http://localhost:3000` and proxies API calls to `http://localhost:8080`.
